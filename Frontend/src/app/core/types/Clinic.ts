export type Clinic = {
  id: string;
  clinicName: string;
  address: string;
  contactNumber: string;
  email: string;
  emails: string[]
  createdDate: string; 
  updatedDate: string; 
  lock : boolean;
  image: string;
};

export { };
